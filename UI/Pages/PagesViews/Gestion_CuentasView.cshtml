@page
@using API.Controllers
@using APPCORE.Security
@{
     if (!SecurityController.Auth(HttpContext.Session.GetString("sessionKey")))
    {
        Response.Redirect("../Security/login");
        return;
    }
    @if (!SecurityController.HavePermission(Permissions.GESTION_MOVIMIENTOS.ToString(), HttpContext.Session.GetString("sessionKey")) )//todo
    {
       Response.Redirect("../Security/login");
        return;
    }    
}
<script src='~/Views/Gestion_CuentasView.js' type='module'></script>
<div id='MainBody'></div>
